export interface Book {
    id: string
    title: string
    author: string
    genere: string
    description: string
    cover_url: string
}

export interface UserType {
    name: string
    email: string
}